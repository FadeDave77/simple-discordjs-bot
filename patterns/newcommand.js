const { MessageEmbed } = require("discord.js");
const vars = require('../variables.js');

module.exports = {
    id: '', 
    aliases: [''], 
    channels: 'any',
    exec: (call) => {
        
    }
};
